# Copyright 2025 Rodericus Ifo Krista
# SPDX-License-Identifier: MIT

services:
  category_service:
    container_name: category_service_${ENV}
    tty: true
    build:
      dockerfile: Dockerfile
      context: .
      args:
        MODE: development
    image: ${SLUG}/category_service:${VERSION}
    environment:
      - ENV=${ENV}
    volumes:
      - ./env:/env
      - ./certs:/certs
    ports:
      - 6061:${SERVER_PORT:-6060}
    restart: on-failure
    networks:
      - backend
    healthcheck:
      test: ["CMD", "nc", "-zv", "localhost", "${SERVER_PORT:-6060}"]
      interval: 10s
      timeout: 5s
      retries: 5

networks:
  backend:
    external: true
    name: ${SLUG}_backend_${ENV}